### 背景・課題

コード複雑化に対して、先に分割方針と完了基準を決めないと、リファクタリングが局所最適になりやすい。

### 提案内容

クライアント/サーバー共通で「責務分離ルール」「削除対象テスト基準」「Done 指標」をドキュメント化する。
対象は `GridCellsScene.ts` と `process_turn_usecase.rs` を中心とする。

### 期待される効果

チーム内で判断がぶれず、後続 Issue のレビュー基準が明確になる。

### 対象範囲

ドキュメント

### 受け入れ条件

- [ ] 責務分割ルール（Scene / UseCase / Domain）が文書化されている
- [ ] テスト削減ルール（削除可/不可）が文書化されている
- [ ] 完了指標（行数・主要シナリオテスト維持）が定義されている

### 補足

この Issue 完了後に後続 Issue へ着手する。
