---
title: フロント ゲーム関連クラス図 3Dモデル版
description: 3D対戦フロントエンドのゲームロジッククラス構成と責務を整理したクラス図
author: TriggerGameCompose Team
ms.date: 2026-07-30
ms.topic: reference
keywords:
  - frontend
  - game logic
  - 3d
  - class diagram
estimated_reading_time: 8
---

## 概要

本ドキュメントは、3D版フロントエンドのゲームロジッククラス構成を整理したものです。

3D版は `GridCellsScene` を継承しつつ、次の専用クラスで責務を分割しています。

* 3D入力は `ThreeDInputController`
* 選択と移動候補表示は `ThreeDSelectionService`
* トリガー方位設定は `ThreeDTriggerSettingController`
* 行動計画と送信判定は `ThreeDTurnPlanner`
* サーバー受信ターンの再生は `ThreeDTurnReplayController`

## 初期化と依存構成

### 3Dシーンの主要依存

```mermaid
classDiagram

class GameGrid {
  +gameDimention
}

class ThreeDGridCellsScene {
  +init()
  +create()
  +executeTurn(turn,motionLabEndTime)
  -createThreeDSelectionServiceDeps()
  -createThreeDFieldViewService()
  -resolveUnitHeightAtGrid(col,row)
}

class GridCellsScene
class ThreeDCharacterManager
class ThreeDInputController
class ThreeDSelectionService
class ThreeDTriggerSettingController
class ThreeDTurnPlanner
class ThreeDTurnReplayController
class ThreeDFieldViewState
class FieldViewService
class ThreeDCharacterPlacementService
class HexUtils

GameGrid --> ThreeDGridCellsScene
ThreeDGridCellsScene --|> GridCellsScene

ThreeDGridCellsScene *-- ThreeDCharacterManager
ThreeDGridCellsScene *-- ThreeDCharacterPlacementService
ThreeDGridCellsScene o-- ThreeDFieldViewState
ThreeDGridCellsScene o-- FieldViewService
ThreeDGridCellsScene o-- ThreeDInputController
ThreeDGridCellsScene o-- ThreeDSelectionService
ThreeDGridCellsScene o-- ThreeDTriggerSettingController
ThreeDGridCellsScene o-- ThreeDTurnPlanner
ThreeDGridCellsScene o-- ThreeDTurnReplayController
ThreeDGridCellsScene --> HexUtils

ThreeDSelectionService --> ThreeDCharacterManager
ThreeDSelectionService --> ThreeDCharacterPlacementService
ThreeDSelectionService --> HexUtils

ThreeDTriggerSettingController --> ThreeDCharacterManager
ThreeDTriggerSettingController --> ThreeDCharacterPlacementService
ThreeDTriggerSettingController --> HexUtils

ThreeDTurnPlanner --> ThreeDCharacterManager
ThreeDTurnPlanner --> HexUtils

ThreeDTurnReplayController --> ThreeDCharacterPlacementService
ThreeDTurnReplayController --> HexUtils
ThreeDTurnReplayController --> ThreeDCharacterManager
```

### クラス責務

| クラス | 役割 |
| --- | --- |
| `ThreeDGridCellsScene` | 3D表示の初期化と、3D専用サービスの依存注入を担当する。 |
| `ThreeDFieldViewState` | 3D地形モデル、空、地面、光源、視界セル表示を管理する。 |
| `ThreeDCharacterManager` | 3Dユニット、ユニット座標、味方/敵状態の管理を担当する。 |
| `ThreeDCharacterPlacementService` | グリッド座標から3Dワールド座標への変換を担当する。 |
| `FieldViewService` | 2D版の視界計算ロジックを3D版でも再利用する。 |

## 行動設定フェーズ

### 選択、移動、トリガー設定、行動履歴

```mermaid
classDiagram

class ThreeDSelectionService {
  +selectCharacter(unitObject)
  +showMovableHexes()
  +moveSelectedCharacterByHighlight(cellMesh)
  +cancelMoveSelection()
  +clearSelection()
}

class ThreeDTriggerSettingController {
  +startTriggerSettingForSelectedUnit()
  +updateCurrentTriggerAngle(directionDeg)
  +completeCurrentTriggerSetting()
  +stopTriggerSetting()
  +getTriggerDirection(unitObject)
}

class ThreeDTurnPlanner {
  +recordActionHistory(unitObject,direction)
  +checkAllCharactersActionPointsCompleted()
  +setMotionLabEnd(endTime)
  +sendMotionLabTurn()
  +resetPlannedTurnState()
}

class ThreeDInputController {
  +bind()
  +unbind()
}

class ThreeDCharacterManager
class ThreeDUnitObject
class ThreeDPlayerCharacterState
class Turn
class Step
class Action

ThreeDInputController --> ThreeDSelectionService
ThreeDInputController --> ThreeDTriggerSettingController

ThreeDSelectionService --> ThreeDCharacterManager
ThreeDSelectionService --> ThreeDPlayerCharacterState
ThreeDSelectionService --> ThreeDUnitObject

ThreeDTriggerSettingController --> ThreeDCharacterManager
ThreeDTriggerSettingController --> ThreeDPlayerCharacterState

ThreeDTurnPlanner --> ThreeDCharacterManager
ThreeDTurnPlanner --> ThreeDPlayerCharacterState
ThreeDTurnPlanner --> Turn

Turn *-- Step
Step *-- Action
```

### 主要メソッド

| メソッド | 役割 |
| --- | --- |
| `ThreeDSelectionService.showMovableHexes` | 選択ユニットの残り行動リソースに基づき移動候補セルを再生成する。 |
| `ThreeDSelectionService.moveSelectedCharacterByHighlight` | セル選択から3D移動アニメーション、座標更新、視界更新までを処理する。 |
| `ThreeDTriggerSettingController.completeCurrentTriggerSetting` | main確定後にsubへ遷移し、sub確定で外部コールバックへ通知する。 |
| `ThreeDTurnPlanner.recordActionHistory` | 3Dユニットの移動履歴とトリガー方位を `Action` 群へ変換する。 |
| `ThreeDTurnPlanner.sendMotionLabTurn` | 未完了行動を補完して `turnExecution` を送信する。 |

## 行動実行フェーズ

### 受信ターンの3D再生

```mermaid
classDiagram

class ThreeDTurnReplayController {
  +executeTurn(turn)
  -executeStep(turn,stepIndex)
  -replayActions(step)
  -replayCombats(step)
  -completeUnitActionPhase(turnNumber)
}

class ThreeDUnitObject {
  +moveTo(position,duration,onComplete,onUpdate)
  +faceToward(position)
  +playAnimation(name,fadeMs,options)
  +updateVisibility(visible)
}

class ThreeDEnemyCharacterState {
  +syncReplayState(params)
  +setBailout(isBailout)
}

class ThreeDPlayerCharacterState {
  +setPosition(position)
  +setActionPoints(points)
  +setRemainSeconds(seconds)
}

class Combat
class Turn
class Step
class FieldViewService {
  +updateVisibility() boolean[][]
}

ThreeDTurnReplayController --> Turn
ThreeDTurnReplayController --> Step
ThreeDTurnReplayController --> Combat
ThreeDTurnReplayController --> ThreeDUnitObject
ThreeDTurnReplayController --> ThreeDEnemyCharacterState
ThreeDTurnReplayController --> ThreeDPlayerCharacterState
ThreeDTurnReplayController --> FieldViewService

Turn *-- Step
Step *-- Combat
```

### クラス責務

| クラス | 役割 |
| --- | --- |
| `ThreeDTurnReplayController` | ステップ順再生、移動アニメーション、撃破反映、ゲーム終了判定、次フェーズ復帰を担当する。 |
| `ThreeDUnitObject` | 3Dモデル、移動、向き、アニメーション再生の表示責務を担当する。 |
| `ThreeDEnemyCharacterState` | 敵ユニットの表示座標同期と撃破状態の管理を担当する。 |
| `ThreeDPlayerCharacterState` | 味方ユニットの現在位置、残り行動リソース、ステップ進行を管理する。 |

## 補足

2D版のクラス図は次のドキュメントを参照してください。

* `docs/03_詳細設計/クラス図/フロント_ゲーム関連クラス図.md`
